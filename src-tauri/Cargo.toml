[package]
name = "yapdp"
version = "0.2.0"
description = "yaPDP - Yet Another PDP-11/70 web emulator packaged as a desktop app"
license = "MIT"
repository = "https://github.com/amesk/yaPDP"
authors = ["Alexei Eskenazi <alexei.eskenazi@gmail.com>"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
name = "yapdp_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
strip = true
